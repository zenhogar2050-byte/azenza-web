# Single Page Application (SPA) Client Fallback for Cloudflare Pages
# Note: Cloudflare Pages serves static HTML files (e.g. /producto/rtafull.html) automatically.
# Dynamic fallback:
/* /index.html 200

